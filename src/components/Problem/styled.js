import styled from "styled-components"

const Problem = styled.div`
  margin: 3rem 1rem;
  font: inherit;
`

const QuizContainer = styled.div`
  text-align: center;
`

const ButtonContainer = styled.div`
  margin: 1rem;
  display: flex;
  justify-content: right;
`

export { Problem, QuizContainer, ButtonContainer}